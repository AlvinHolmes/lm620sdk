#!/bin/sh

set -u
set -e
trap onexit INT
trap onexit TERM
trap onexit EXIT

TMPDIR=

onexit()
{
	if [ ! "$TMPDIR" = "" ]; then
		rm -rf $TMPDIR
	fi
}

if [ -f libjpeg-turbo-3.0.1.RISCV32.rpm ]; then
	rm -f libjpeg-turbo-3.0.1.RISCV32.rpm
fi

umask 022
TMPDIR=`mktemp -d /tmp/libjpeg-turbo-build.XXXXXX`

mkdir -p $TMPDIR/RPMS
ln -fs `pwd` $TMPDIR/BUILD
rpmbuild -bb --define "_blddir $TMPDIR/buildroot" --define "_topdir $TMPDIR" \
	--target RISCV32 pkgscripts/rpm.spec; \
cp $TMPDIR/RPMS/RISCV32/libjpeg-turbo-3.0.1-20241108.RISCV32.rpm \
	libjpeg-turbo-3.0.1.RISCV32.rpm
